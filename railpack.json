{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm ci"]
    },
    "build": {
      "commands": ["...", "npm prune --omit=dev --ignore-scripts"]
    }
  }
}
